export type Category = {
    id: string
    name: string
    slug: string
}

export type Chapter = {
    filename: string
    chapter_name: string
    chapter_title: string
    chapter_api_data: string
}